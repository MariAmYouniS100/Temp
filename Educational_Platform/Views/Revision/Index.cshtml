@model IEnumerable<Revision>

<!-- Page Header -->
<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-4">
    <div class="mb-3 mb-md-0">
        <h1 class="h2 mb-1">
            <i class="fas fa-users me-2 text-primary"></i>revision Management
        </h1>
        <p class="text-muted mb-0">View and manage all revision records</p>
    </div>
    <div>
        <a class="btn btn-primary" asp-action="Create">
            <i class="fas fa-plus-circle me-2"></i>Add New revision
        </a>
    </div>
</div>

<!-- Search and Filter Card -->
<div class="card shadow-sm mb-4">
    <div class="card-body">
        <form asp-action="Index" class="row g-3">
            <div class="col-md-8">
                <div class="input-group">
                    <span class="input-group-text bg-transparent">
                        <i class="fas fa-search text-muted"></i>
                    </span>
                    <input type="search" class="form-control" placeholder="Search revision..." name="search">
                    <button class="btn btn-primary" type="submit">
                        <i class="fas fa-filter me-1"></i>Filter
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>


<div class="table-responsive">
    <table class="table table-hover mb-0">
        <thead class="table-light">
            <tr>
                <th class="ps-4">ID</th>
                <th>Title</th>
                <th>Supporting Files</th>
                <th>Video URL</th>
                <th>Schedule Date</th>
                <th>Course</th>
                <th class="text-end pe-4">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var revision in Model)
            {
                <tr>
                    <td class="ps-4 fw-semibold align-middle">#@revision.ID</td>
                    <td class="align-middle">
                        <div class="fw-semibold">@revision.Title</div>
                    </td>
                    <td class="align-middle">
                        @if (!string.IsNullOrEmpty(revision.SupportingFiles))
                        {
                            <div class="text-truncate" style="max-width: 200px;" data-bs-toggle="tooltip" title="@revision.SupportingFiles">
                                @revision.SupportingFiles
                            </div>
                        }
                        else
                        {
                            <span class="text-muted">No files</span>
                        }
                    </td>
                    <td class="align-middle">
                        @if (!string.IsNullOrEmpty(revision.VideoURL))
                        {
                            <div class="text-truncate" style="max-width: 200px;" data-bs-toggle="tooltip" title="@revision.VideoURL">
                                @revision.VideoURL
                            </div>
                        }
                        else
                        {
                            <span class="text-muted">No video</span>
                        }
                    </td>
                    <td class="align-middle">@revision.ScheduleDate</td>
                    <td class="align-middle">
                        
                    </td>
                    <td class="align-middle">
                        <span class="badge bg-light text-dark">
                            @(revision.Course?.Title ?? "N/A")
                        </span>
                    </td>
                    <td class="text-end pe-4 align-middle">
                        <div class="btn-group btn-group-sm" role="group">
                            <a asp-action="Details" asp-route-id="@revision.ID"
                               class="btn btn-outline-primary" data-bs-toggle="tooltip" title="View Details" aria-label="View Details">
                                <i class="fas fa-eye"></i>
                            </a>
                            <a asp-action="Edit" asp-route-id="@revision.ID"
                               class="btn btn-outline-warning" data-bs-toggle="tooltip" title="Edit" aria-label="Edit">
                                <i class="fas fa-edit"></i>
                            </a>
                            <form asp-action="Delete" asp-route-id="@revision.ID" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-outline-danger"
                                        data-bs-toggle="tooltip" title="Delete" aria-label="Delete"
                                        onclick="return confirm('Are you sure you want to delete @revision.Title?')">
                                    <i class="fas fa-trash-alt"></i>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>